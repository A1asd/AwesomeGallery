class ViewModeManager {
	static FOLDER = 'folder'
	static TAGS = 'tags'
	static GALLERY = 'gallery'

	static VIEWMODES = [
		ViewModeManager.FOLDER,
		ViewModeManager.TAGS,
		ViewModeManager.GALLERY,
	]
}

export default ViewModeManager;
